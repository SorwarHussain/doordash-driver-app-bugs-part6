# doordash-driver-app-bugs-part6
When accepting **two combined (stacked) orders**, tapping the **“Directions”** button for the **first pickup location** sometimes fails to open navigation. The button appears responsive, but no navigation screen or external map opens, preventing the driver from navigating to the store from within the app.
